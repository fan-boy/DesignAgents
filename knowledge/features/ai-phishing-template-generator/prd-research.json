{
  "feature_name": "AI Phishing Template Generator",
  "feature_slug": "ai-phishing-template-generator",
  "goal": "Let a security analyst or admin generate a high-fidelity phishing simulation template in roughly 10 seconds from either a free-text scenario description or an uploaded real email, without depending on a central content team.",
  "user_segments": ["Security Analyst / Admin (creator)", "Admin (deploy-permitted role)"],
  "success_metrics": [],
  "constraints": [
    "Generation completes within ~10 seconds",
    "Uploaded .eml and screenshot content must be sandboxed, sanitized, and isolated per workspace",
    "RBAC gates view, edit, and deploy separately",
    "All AI generation and edit actions must be logged for auditability"
  ],
  "assumptions": [
    "Redaction is text-based only for v1 by decision; sensitive content embedded in images is not detected or flagged",
    "Copy an Email and Describe a Scenario carry the same permission requirements by decision — no separate gate for raw email upload",
    "No mandatory human review/attestation step is required before deploying an email-sourced template for v1 by decision",
    "Manual edits are always preserved; difficulty only applies at generation or explicit Regenerate, never as a live re-adjustment on an edited draft",
    "A template library exists as a save destination with no further behavior specified"
  ],
  "gaps": [
    "No retention, deletion, or training-pipeline-exclusion policy for uploaded raw .eml/screenshot content",
    "Template library has no defined list, search, filter, sort, or delete behavior",
    "Phishing category list origin (fixed enum vs. admin-configurable) is unspecified",
    "Audit logging covers actions but not whether prior draft versions are recoverable",
    "UI copy must not overstate redaction coverage given image-embedded content is out of scope for v1 redaction"
  ],
  "edge_cases": [
    "Generation fails outright distinct from exceeding the ~10s target",
    "Sanitization strips or rejects part of an upload with no described analyst-facing message",
    "Template library empty state on first use",
    "View-only role opens a template in the editor",
    "Copy an Email permission separate from Describe a Scenario permission",
    "Cross-workspace access attempt on a template ID",
    "Sparse or single-word scenario prompt with no minimum length enforced",
    "Plaintext-only .eml with no layout to preserve",
    "Duplicate template name within a workspace",
    "Template library at scale with no search or filter",
    "Regenerate action distinct from difficulty-triggered re-adjustment, with no overwrite warning",
    "No delete flow described for templates",
    "Editing on mobile/tablet not addressed (preview only is described)",
    "Drag-and-drop block reordering has no keyboard-operable alternative"
  ],
  "critical_questions": [
    { "owner": "Eng", "question": "Where are raw .eml uploads and screenshots stored after generation, for how long, and are they excluded from logging or model-training pipelines given they may contain real PII?" },
    { "owner": "PM", "question": "What are the actual success metrics for this feature — adoption, time saved versus the prior content-team workflow, or percentage of AI drafts deployed with no manual edits?" },
    { "owner": "PM", "question": "Is there a delete flow for templates, and what happens on the campaign launcher side if a linked template is deleted or edited?" },
    { "owner": "Eng", "question": "Is the phishing category list a fixed enum or admin-configurable/extensible with custom values?" }
  ],
  "design_risks": [
    "Redaction is text-based only for v1 (decided) — UI copy claiming templates are 'sanitized' or fully redacted must carry a caveat, or customers will assume image content is covered when it isn't",
    "Unscoped raw-content ingestion via Copy an Email creates a compliance liability with no stated retention limit — retention/deletion policy for uploaded .eml content remains unresolved",
    "Underspecified template library risks either late scope growth in eng estimation or an unusable flat list at moderate volume"
  ],
  "teaching_notes": [
    "knowledge/features/aep-library/ is the closest existing precedent: AI-generated artifact created via guided setup, refined in a Test & Refine step, backed by a filterable library table with status flow, search, row actions, and an empty state — model this feature's library on it directly",
    "Apply the 'errors inform, don't punish' principle to generation failures and sanitization rejections: tell the analyst specifically what was flagged and why",
    "Trust & Safety principle applies directly: this feature claims to redact sensitive data and produce safe links, so any gap between that claim and actual behavior is a trust failure, not just a bug",
    "Apply Visibility of System Status (heuristics.md #1) to the ~10s generation window and to sanitization — show progress, never a silent wait"
  ],
  "last_updated": "2026-07-08"
}
